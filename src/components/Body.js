import React, {useState} from "react";
//Components
import Slideshow from "./Slideshow";

const Body = () => {
    return(
        <div>

            <Slideshow />
        </div>
    );
};

export default Body;